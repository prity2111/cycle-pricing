package com.social91.cycle;

/**
 * getPrice need to be determined for all the components of cycle therefore,
 * created within interface so that each component can implement it on their own way
 */
public interface CycleComponent {
    double getPrice();

}
